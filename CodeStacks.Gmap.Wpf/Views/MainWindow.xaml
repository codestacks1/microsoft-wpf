<Window x:Class="xiaowen.codestacks.wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:xiaowen.codestacks.wpf.Views"
        xmlns:ctrl ="clr-namespace:xiaowen.codestacks.wpf.Views.UserControls"
        xmlns:xwwiewmodels="clr-namespace:xiaowen.codestacks.wpf.ViewModels"
        xmlns:xwprism="http://www.codeplex.com/prism"
        d:DataContext="{d:DesignInstance xwwiewmodels:MainWindowViewModel,IsDesignTimeCreatable=True}"
        xwprism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="768" Width="1366" MinHeight="768" MinWidth="1366" Icon="/CodeStacks.Gmap.Wpf;component/Untitled-1.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl xwprism:RegionManager.RegionName="ContentRegion" />

        <GroupBox Header="Operator">
            <ctrl:MyMapOptControl />
        </GroupBox>

        <GroupBox Grid.Row="1" Name="mapgroup" Header="xiaowen.codestacks.wpf.Demo" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
            <ctrl:MyMapControl />
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Geo Infomation" Margin="0,0,0,10">
            <ctrl:GetAnchorData x:Name="_MyMapAnchorData" />
        </GroupBox>

    </Grid>
</Window>
